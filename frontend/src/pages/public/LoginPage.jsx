import { useState } from "react";
import { Link, useLocation, useNavigate } from "react-router-dom";
import { useAuth } from "../../contexts/AuthContext";

export default function LoginPage() { const { signIn } = useAuth(); const navigate = useNavigate(); const location = useLocation(); const [form, setForm] = useState({ email: "", password: "" }); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); const submit = async (event) => { event.preventDefault(); setBusy(true); setError(""); try { await signIn(form); navigate(location.state?.from?.pathname || "/account", { replace: true }); } catch (reason) { setError(reason.message); } finally { setBusy(false); } }; return <AuthForm title="Chào mừng trở lại" hint="Đăng nhập để tiếp tục hành trình của bạn." onSubmit={submit} error={error} busy={busy} fields={[{ name: "email", label: "Email", type: "email" }, { name: "password", label: "Mật khẩu", type: "password" }]} form={form} setForm={setForm}><p className="form-footer">Chưa có tài khoản? <Link to="/register">Đăng ký</Link></p></AuthForm>; }

export function AuthForm({ title, hint, onSubmit, error, busy, fields, form, setForm, children }) { return <section className="auth-page"><div className="auth-panel"><p className="eyebrow">LÁ BÀI</p><h1>{title}</h1><p className="lead">{hint}</p><form onSubmit={onSubmit}>{fields.map((field) => <label key={field.name}>{field.label}<input required type={field.type} value={form[field.name]} onChange={(event) => setForm({ ...form, [field.name]: event.target.value })} /></label>)}{error && <p className="form-error">{error}</p>}<button className="button button-wide" disabled={busy}>{busy ? "Đang xử lý..." : "Tiếp tục"}</button>{children}</form></div></section>; }
